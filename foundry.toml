[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.26"
# Arc docs: set evm_version to shanghai (closest match to Arc's Osaka baseline)
evm_version = "shanghai"
optimizer = true
optimizer_runs = 2000

[rpc_endpoints]
arc_testnet = "https://rpc.testnet.arc.io"
