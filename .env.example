# ── Testnet (current) ──
# Deployer key funded with Arc testnet USDC (faucet: https://faucet.circle.com)
PRIVATE_KEY=
# Optional: protocol fee recipient (defaults to deployer)
TREASURY=
ARC_TESTNET_RPC=https://rpc.testnet.arc.io

# ── Mainnet (Arc, chain 5042, public launch 2026-09-16) ──
# Fill these in for the mainnet deploy. Use a FRESH key, never the testnet one.
ARC_MAINNET_RPC=
# Canonical Uniswap V4 on Arc mainnet (Uniswap ships v4 on Arc — do NOT self-deploy).
#   PoolManager is deterministic across chains; confirm on Arcscan before use.
POOL_MANAGER=0x8366a39cc670b4001a1121b8f6a443a643e40951
POSITION_MANAGER=          # canonical Arc v4 PositionManager — fetch at launch (see runbook)
PERMIT2=0x000000000022D473030F116dDEE9F6B43aC78BA3
# Protocol fee recipient — set to the treasury/multisig, NOT the deployer.
PROTOCOL_RECIPIENT=
# Multisig (Gnosis Safe) that will own the protocol after the smoke test.
MULTISIG=
# Production launch economics (native USDC, whole units). Review before deploy.
LAUNCH_FEE_USDC=1
PHANTOM_USDC=4000
GRADUATION_USDC=10000
# Deployed addresses (fill from the deploy output, used by TransferOwnership).
FACTORY=
HOOK=
VAULT=
LOCKER=
