out/
cache/
broadcast/
.env
.DS_Store

# tooling / build artifacts
.claude/
web/tsconfig.tsbuildinfo

# every env file except the template (mainnet keys live in .env.<chain>-mainnet)
.env.*
!.env.example
!.env.*.example
